<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.searchnearestattractionapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppClass"
        android:allowBackup="true"
        android:icon="@drawable/suburban"
        android:label="@string/app_name"
        android:roundIcon="@drawable/suburban"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.schedule_act.ScheduleActivity" />
        <activity android:name=".ui.TestActivity">

        </activity>
        <activity android:name=".MapAct" />
        <activity
            android:name=".ui.add_points_act.AddPointsActivity"
            android:theme="@style/Theme.Transparent" />
        <activity android:name=".ui.main_act.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>